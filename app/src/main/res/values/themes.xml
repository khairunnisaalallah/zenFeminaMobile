<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication1" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyApplication1" parent="Base.Theme.MyApplication1" />
    <style name="FragmentActivityTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Atur atribut-atribut lain yang Anda perlukan -->
        <item name="android:statusBarColor" tools:targetApi="1">@color/colorPrimaryDark</item>
    </style>
    <style name="ThemeMaterialCalender" parent="ThemeOverlay.MaterialComponents.MaterialCalendar">
        <item name="android:windowFullscreen">false</item>
    </style>
</resources>
